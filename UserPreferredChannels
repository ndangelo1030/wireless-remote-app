import user from User
import fchannels from User
  
  class ViewFavorites:
  def find(username, fchannels):
  return username(user, fchannels)
  
  classAddFavorites:
def add(fchannels, blockedchannels):
return PreferredChannels(fchannels, blockedchannels)
  
